import React from 'react';
import propTypes from 'prop-types';
import { isNumeric } from '../../Helper';

const Hand = ({ cards }) => <div />;

Hand.propTypes = { cards: propTypes.string.isRequired };

export default Hand;
